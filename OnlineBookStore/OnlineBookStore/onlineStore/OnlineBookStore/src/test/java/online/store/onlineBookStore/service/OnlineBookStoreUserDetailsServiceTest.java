/*
package online.store.onlineBookStore.service;

import online.store.onlineBookStore.models.entities.Cart;
import online.store.onlineBookStore.models.entities.Role;
import online.store.onlineBookStore.models.entities.User;
import online.store.onlineBookStore.models.enums.GenderEnum;
import online.store.onlineBookStore.models.repositories.UserRepository;
import online.store.onlineBookStore.models.services.OnlineBookStoreDetailsService;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import javax.persistence.*;
import java.util.Collection;

@ExtendWith(MockitoExtension.class)
class OnlineBookStoreUserDetailsServiceTest {

    @Mock
    private UserRepository mockUser;

    private OnlineBookStoreDetailsService toTest;
*/

/*
    @BeforeEach
    void setUp() {
        toTest = new OnlineBookStoreDetailsService(
        mockUser
        );
    }

    @Test
    void testLoadUserByUsername_UserExists() {
        User testUser = new User()
                .setUsername("testOv")
                .setFirstName("Test")
                .setLastName("Testov")
                .setAge(5)
                .setEmail("testov@gmail.com")
                .setPassword("1234")
                .setGender(GenderEnum.MALE)
                .setPhoneNumber("+3590967512")
                .setRole()

    }

    @Test
    void testLoadUserByUsername_UserDoesNotExists() {

    }
}
*/
